asyncpg>=0.29.0
openai>=1.40.0
pgvector>=0.3.2
python-telegram-bot>=21.6
python-dotenv>=1.0.1
telethon>=1.36.0
# Local embeddings (no API key). all-MiniLM-L6-v2 -> 384-dim TOPIC vectors;
# StyleDistance/styledistance -> 768-dim STYLE vectors (agent/style_embed.py).
# sentence-transformers pulls transformers + torch, which the style model uses.
sentence-transformers>=3.0.0
